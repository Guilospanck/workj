layout {
  pane split_direction="horizontal" {
    pane split_direction="vertical" {
      pane size="70%" { }

      pane {
        command "kiro-cli"
      }
    }

    // ───────────── BOTTOM (full width) ─────────────
    pane size="20%" { }
  }
}
